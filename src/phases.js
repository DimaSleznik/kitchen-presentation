/* ============================================================================
   PHASES — «Фазы»: вертикальная лента карточек, по одной на фазу.
   Дизайн «журнальный сплит»: слева цветной корешок (что это / сколько длится),
   справа — кто что делает + КРУПНО «что увидим и получим в конце фазы».
   Цветовой код корешка: демо-фаза (показываем заказчику) — терракота, внутренняя — дуб.
   Данные берём из board.js (ITERATIONS · TASKS · LANES), чтобы план не разъезжался.
   ========================================================================== */

import { ITERATIONS, TASKS, LANES } from './board.js';

const laneById = Object.fromEntries(LANES.map((l) => [l.id, l]));

/* Развёрнутый, человеческий «что увидим в конце фазы» — главный смысл страницы.
   Пишем глазами обычного пользователя планнера: что станет видно и можно потрогать.
   Сжатые output[] из board.js — это «сухой» вариант; здесь делаем его выпуклым. */
const OUTCOME = {
  i0: 'Показывать заказчику пока нечего — но фундамент заложен по-настоящему. Выбрана чистая архитектура (прототип — это черновик-референс, а не основа), а реальный каталог Häcker — больше 10 000 товаров с ценами и допустимыми размерами — уже лежит в нашей базе. Дизайн нарисовал первые экраны настройки шкафа, заложено место под ИИ, и всё накрыто авто-проверками: дальше можно менять что угодно, не боясь молча сломать готовое.',
  i1: 'Снаружи каталог выглядит ровно как раньше — но внутри он переехал на единый слой шаблонов: вся геометрия и детали идут через одну точку, а данные и 3D-детали отдаются сервисами отдельно от программы. Каталог открывается плавно, отделки переключаются мгновенно, материалы — общий набор, а не копии под каждый цвет. Параллельно стартовало обучение собственной модели для будущих фотопревью. Тесты подтверждают: невидимый переезд не изменил каталог ни на пиксель.',
  i2: 'Первое «вау» для заказчика: открываешь нижний шкаф и тянешь ползунок — ширина, число ящиков и сторона петли меняются, и шкаф перестраивается вживую прямо на экране, без единого нового файла. Его форма совпадает с заводской Häcker, потому что мы перенесли реальные формы из старого редактора. Панель настройки понятная и отзывчивая, а арифметические тесты подтверждают: размеры и вырезы сходятся. Гибрид доказан на самом тяжёлом семействе шкафов.',
  i3: 'Каталог теперь — это готовые товары-пресеты, которые ставишь в проект одним кликом, и собрать кухню можно не только мышкой, но и словами: говоришь ассистенту «добавь нижний шкаф с тремя ящиками» — и он достраивает её сам, строго в рамках заводских правил (размеры, сетка, совместимость фасада и корпуса). Движок правил — главный судья: ни человек, ни ИИ не могут собрать недопустимое. А первые реальные заказы уже сходятся со старой системой по размеру, цене и составу.',
  i4: 'Каталог становится полным: навесные, высокие пеналы и угловые шкафы — можно собрать настоящую кухню целиком. Заказчик задаёт нестандартную ширину под свою стену и сразу видит, допустимо ли это: зелёная подсказка или мягкая граница вместо сухой ошибки, а шкаф тут же перестраивается под новый размер. И главное для завода: команды, по которым станки режут детали, перенесены из старого редактора и сверены буква в букву — фабрика получает ровно те же детали. «Миграция без потерь» теперь доказана, а не обещана.',
  i5: 'Финал: каталог наполняется быстрее — ИИ готовит черновики товаров из прайс-листа под проверку человека, — а каждый шкаф получает фотореалистичное превью, сгенерированное нашей обученной моделью в едином фирменном стиле. Геометрия остаётся нашей и точной, красивая картинка — за ИИ. Готовую кухню видно целиком и можно выгрузить одним файлом — для просмотра, шаринга или на производство.',
};

export function mountPhases(root) {
  if (root.dataset.mounted) return;
  root.dataset.mounted = '1';

  const cards = ITERATIONS.map((it, idx) => {
    const tasks = TASKS.filter((t) => t.iter === it.id);
    const minis = tasks.map((t) => {
      const lane = laneById[t.lane];
      return `<div class="ph-mini">
        <span class="ph-mini-role"><span class="ph-ic">${lane.icon}</span>${lane.label}</span>
        <b>${t.title}</b>
        <span class="ph-mini-blurb">${t.hook}</span>
      </div>`;
    }).join('');

    const num = String(idx).padStart(2, '0');
    const delay = Math.min(idx, 4) * 70;
    return `<article class="ph-card${it.demo ? ' demo' : ''} reveal" style="transition-delay:${delay}ms">
      <div class="ph-rail">
        <span class="ph-num">${num}</span>
        <div class="ph-railband">
          <h2 class="ph-title">${it.sub}</h2>
          <span class="ph-weeks">${it.name} · ${it.weeks}</span>
          <p class="ph-why">${it.why}</p>
        </div>
        <span class="ph-flag">${it.demo ? '★ показываем заказчику' : 'внутренний этап'}</span>
      </div>
      <div class="ph-main">
        <p class="ph-subh">Кто что делает</p>
        <div class="ph-grid">${minis}</div>
        <div class="ph-out">
          <span class="ph-out-label">${it.demo ? 'Что увидим и покажем в конце →' : 'Что получим в конце →'}</span>
          <p>${OUTCOME[it.id] || it.output}</p>
        </div>
      </div>
    </article>`;
  }).join('');

  root.innerHTML = `<div class="wrap ph-wrap">
    <header class="ph-head reveal">
      <div class="eyebrow">План работ · по фазам · что увидим на каждом шаге</div>
      <h1>Шесть фаз — от фундамента до <em>живого планнера кухонь</em></h1>
      <p class="lede">Каждая карточка — одна фаза. Слева — что это и сколько длится; справа — кто что делает
        и, главное, <b>что мы получим и увидим в конце фазы</b>. Значок ★ — то, что показываем заказчику вживую.</p>
    </header>
    <div class="ph-list">${cards}</div>
    <div class="ph-foot reveal">Полный горизонт — около года (≈ 54 недели) силами небольшой команды.
      Детальная доска с ролями и зависимостями — на вкладке <a href="#/board">«План работ»</a>.</div>
  </div>`;
}
